# AI Claims Assistant

This project now includes a frontend built with **AngularJS**.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Frontend features

- Create a new claim from a form.
- View submitted claims in a list.
- AngularJS controller-driven state updates.
